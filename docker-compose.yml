version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai-resume-analyzer:compose
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PORT=${PORT:-8000}
    restart: unless-stopped
